language: python
env:
  global:
  - PIP_DEPS="pytest coveralls pytest-cov pep8"
python:
- '3.6'
install:
- travis_retry pip install $PIP_DEPS
- travis_retry pip install -r requirements.txt
branches:
  only:
  - master
script:
- pep8 `find . -name \*.py | grep -v setup.py | grep -v version.py | grep -v __init__.py
  | grep -v /doc/ | grep -v /data/`
- mkdir for_test
- cd for_test
- py.test --pyargs sspinn --cov-report term-missing --cov=pv_cell
after_success:
- coveralls
